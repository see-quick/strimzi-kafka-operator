/**
 * Enum, which holds all choices, which is able to execute in the testing
 */
public enum Choices {

    ClusterOperator,
    Kafka,
    KafkaUser,
    KafkaTopic,
    KafkaConnect,
    KafkaStreams,
    KafkaConnectS2I
}
